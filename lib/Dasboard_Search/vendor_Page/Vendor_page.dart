import 'package:flutter/material.dart';

class VendorPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Vendor Page'),
      ),
      body: Center(
        child: Text('Vendor Content Here'),
      ),
    );
  }
}