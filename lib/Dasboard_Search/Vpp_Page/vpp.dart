import 'package:flutter/material.dart';

class VPPPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('VPP Page'),
      ),
      body: Center(
        child: Text('VPP Content Here'),
      ),
    );
  }
}
