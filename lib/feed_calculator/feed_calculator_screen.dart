import 'package:flutter/material.dart';

class FeedCalculatorScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('feed Calculator'),
      ),
      body: Center(
        child: Text('This is the Feed Calculator Screen'),
      ),
    );
  }
}
